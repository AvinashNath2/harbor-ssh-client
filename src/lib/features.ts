/**
 * Feature flags — set AI to true when the AI integration is ready to release.
 * All AI-facing UI gates on this single constant so one flip enables everything.
 */
export const FEATURES: { AI: boolean } = {
  AI: false,
};
